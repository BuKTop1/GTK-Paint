# PFL - GTK+ приложение для рисования

## 📋 Требования

- GCC или Clang компилятор
- GTK+ 3.0 или выше
- Инструменты сборки (make или cmake)

### Установка зависимостей (Debian/Ubuntu)

```bash
sudo apt-get update
sudo apt-get install build-essential
sudo apt-get install libgtk-3-dev
sudo apt-get install pkg-config
```

## 🔨 Способ 1: Сборка с Makefile (рекомендуется)

### Простая сборка
```bash
cd /home/buktop/projects/PFL
make
```

### Альтернативные команды
```bash
# Отладочная версия (с символами отладки)
make debug

# Оптимизированная версия
make release

# Собрать и запустить
make run

# Запустить в GDB отладчике
make debug-run

# Очистить объектные файлы
make clean

# Полная пересборка
make rebuild

# Проверить синтаксис без сборки
make check

# Справка
make help
```

## 🔨 Способ 2: Сборка с CMake

### Подготовка
```bash
cd /home/buktop/projects/PFL
mkdir build
cd build
cmake ..
```

### Сборка
```bash
make
```

### Запуск
```bash
./PFL
```

### Альтернативные типы сборки
```bash
# Отладочная версия
cmake -DCMAKE_BUILD_TYPE=Debug ..
make

# Релиз версия
cmake -DCMAKE_BUILD_TYPE=Release ..
make
```

## 📁 Структура проекта

```
PFL/
├── src/              # Исходные файлы (.c)
│   ├── main.c        # Главный файл приложения
│   ├── menu_events.c
│   ├── Drawing_events.c
│   └── Color_buttons.c
├── include/          # Заголовочные файлы (.h)
│   └── PFL.h
├── asserts/          # Ресурсы (изображения, UI макеты)
├── build/            # Директория для сборки (создаётся автоматически)
├── Makefile          # Конфиг для make
├── CMakeLists.txt    # Конфиг для cmake
└── README.md         # Этот файл
```

## 🐛 Отладка

### Сборка с отладочной информацией
```bash
make debug
```

### Запуск в GDB
```bash
make debug-run
```

или

```bash
gdb ./build/PFL
(gdb) run
```

## 🧹 Очистка

```bash
# Удалить скомпилированные файлы
make clean

# Удалить директорию build полностью (для CMake)
rm -rf build/
```

## ⚙️ Флаги компиляции

- `-Wall` - включить все предупреждения
- `-Wextra` - дополнительные предупреждения
- `-std=c11` - использовать стандарт C11
- `-g` - добавить информацию для отладки
- `-O0` - без оптимизации (отладка)
- `-O2` - оптимизация уровня 2 (릴리즈)

## 🔗 Полезные ссылки

- [GTK+ 3.0 Документация](https://developer.gnome.org/gtk3/)
- [GCC Флаги](https://gcc.gnu.org/onlinedocs/gcc/Invoking-GCC.html)
- [Make Manual](https://www.gnu.org/software/make/manual/)
- [CMake Documentation](https://cmake.org/documentation/)

## 📝 Заметки

- Выходной файл сохраняется в `build/PFL`
- Объектные файлы (.o) хранятся в `build/obj/`
- Для изменения флагов компилятора отредактируйте Makefile или CMakeLists.txt
